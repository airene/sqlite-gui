# ── 前端依赖 / 构建产物 ──────────────────────
node_modules/
dist/

# ── Rust / Tauri 构建产物 ─────────────────────
src-tauri/target/
src-tauri/gen/schemas/

# 由 build/icon.svg 生成的中间图（src-tauri/icons/ 是最终产物，已提交）
build/icon-1024.png

# ── SQLite 运行时临时文件（读取 WAL 库时产生）──
*.sqlite-wal
*.sqlite-shm
*.sqlite3-wal
*.sqlite3-shm
*.db-wal
*.db-shm

# ── 日志 / 环境变量 ────────────────────────────
*.log
npm-debug.log*
.env
.env.local
.env.*.local

# ── macOS / 编辑器 ────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp
*~

# 测试库含敏感数据（认证哈希/令牌），不纳入版本控制
starcontrol-auth.sqlite3
